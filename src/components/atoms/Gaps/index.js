import React from 'react'
import { View } from 'react-native'

export default function Gaps({ heights, widths }) {
    return (
        <View style={{ height: heights, width: widths }}>
        </View>
    )
}

